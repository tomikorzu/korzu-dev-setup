// App-level identity used across metadata, sitemap, robots, and the OG image.
// Edit this per project, same spirit as src/theme/project.config.ts for styling.
export const siteConfig = {
  name: "Korzu",
  description: "A reusable dev setup with Next.js, MUI, and GSAP.",
  url: "https://example.com",
  locale: "en_US",
  twitterHandle: "@yourhandle",
} as const;
